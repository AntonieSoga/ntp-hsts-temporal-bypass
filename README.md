# ntp-hsts-temporal-bypass

This repository contains a Proof of Concept (PoC) implementation of a time-shifting attack against HTTP Strict Transport Security (HSTS) by exploiting the Network Time Protocol (NTP).
